# [passgen.draylegend.dev](https://passgen.draylegend.dev)

✨ Generated by [Nx](https://nx.dev) ✨.

## Initial setup

Run once after cloning the repository:

```sh
chmod +x .git-hooks/* && git config core.hooksPath .git-hooks
```
